<%= form_with model: @achieve, local: true do |form| %>
  <%= render 'shared/errors', resource: @achieve %>

  <p>
    <%= form.label :description %><br>
    <%= form.text_field :description %>
  </p>

  <p><%= form.submit %></p>
<% end %>